defmodule SentientwaveAutomataTemporalTest do
  use ExUnit.Case
  doctest SentientwaveAutomataTemporal

  test "greets the world" do
    assert SentientwaveAutomataTemporal.hello() == :world
  end
end
